# Max-framing Phase B: register + apply (runs after seqplatesolve, even if plate solve failed)
# Uses image-matching registration as fallback if WCS headers are missing.

requires 1.4.0

cd process
register light -2pass
seqapplyreg light -framing=max
